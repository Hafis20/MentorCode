import { ChangetopositivePipe } from './changetopositive.pipe';

describe('ChangetopositivePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangetopositivePipe();
    expect(pipe).toBeTruthy();
  });
});
