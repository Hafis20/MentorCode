import { VedioDirective } from './vedio.directive';

describe('VedioDirective', () => {
  it('should create an instance', () => {
    const directive = new VedioDirective();
    expect(directive).toBeTruthy();
  });
});
